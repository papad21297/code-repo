#include <stdio.h>

void solve_puzzle(void);

int main(int argc, char *argv[])
{
    if (argc == 2)
        solve_puzzle();
    else
        write(1, "invalid amount of input(s)", 26);
    return (0);
}

void solve_puzzle(void)
{
    // code...
    int n;
    int i;

    n = 4
    i = 0;
    while (i < n)
    {
        printf("%d\n", %d);
        i++;
    }
}